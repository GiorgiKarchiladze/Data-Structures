package com.exam;


import com.exam.DataStructures.Queue;
import com.exam.DataStructures.Stack;

public class App
{
    public static void main( String[] args )
    {
        //tests for stack
        //Stack stack = new Stack(10);

        /*
        tests for stack push and show
        for (int i = 10; i > 0; i--) {
            stack.push(i);
        }
        stack.showStack();
         */

        /*
        test for stack isFull and isEmpty
        for (int i = 10; i > 0; i--) {
            stack.push(i);
        }
        System.out.println(stack.isEmpty());
        System.out.println(stack.isFull());
         */

        /*
        test for stack pop
        for (int i = 10; i > 0; i--) {
            stack.push(i);
        }
        stack.pop();
        stack.showStack();
         */

        /*
        test for stack peek
        for (int i = 10; i > 0; i--) {
            stack.push(i);
        }
        System.out.println(stack.peek());
         */

        //tests for queue
        //Queue queue = new Queue(10);

        /*
        tests for enqueue and show
        for(int i=0;i<10;i++){
            queue.enqueue(i);
        }
        queue.showQueue();
         */

        /*
        test for queue isFull and isEmpty
        for(int i=0;i<10;i++){
            queue.enqueue(i);
        }
        System.out.println(queue.isEmpty());
        System.out.println(queue.isFull());
         */

        /*
        test for dequeue
        for(int i=0;i<10;i++){
            queue.enqueue(i);
        }
        queue.dequeue();
        queue.showQueue();
         */

        /*
        tests for queue head() and tail()
        for(int i=0;i<10;i++){
            queue.enqueue(i);
        }
        System.out.println(queue.head());
        System.out.println(queue.tail());
         */

    }
}
